{
  "name": "xtb-analyser-frontend",
  "version": "0.0.1",
  "main": "dist/main.js",
  "scripts": {
    "build": "bun build --target=browser --env=inline --outdir=./dist/ index.html",
    "test": "bun test",
    "format": "prettier --check .",
    "format:write": "bun run format --write"
  },
  "files": [
    "./dist"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@types/d3": "^7.4.3",
    "typescript": "^5",
    "prettier-config": "workspace:settings"
  },
  "dependencies": {
    "@observablehq/stdlib": "^5.8.8",
    "d3": "^7.9.0",
    "date-fns": "^4.1.0",
    "excelize-wasm": "0.0.9",
    "zod": "^3.25.51"
  }
}
