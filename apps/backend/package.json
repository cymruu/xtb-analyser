{
  "name": "xtb-analyser",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "bun build --target=bun --outdir=dist/ --outfile=main.js ./src/main.ts",
    "dev": "bun run --hot src/main.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "4.7.4"
  }
}
