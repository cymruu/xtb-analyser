{
  "name": "xtb-analyser",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start": "bun run dist/server.js",
    "build": "bun build --target=bun --sourcemap=linked --outdir=dist/ --outfile=server.js ./src/server.ts",
    "dev": "bun --watch run src/server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "hono": "^4.7.11",
    "zod": "^3.25.56"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.9.0",
    "typescript": "^5"
  }
}
