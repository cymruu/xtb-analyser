{
  "name": "xtb-analyser",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start": "bun run dist/server.js",
    "build": "bun build --target=bun --outdir=dist/ --outfile=server.js ./src/server.ts",
    "dev": "bun run --hot src/server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "hono": "4.7.4"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  }
}
