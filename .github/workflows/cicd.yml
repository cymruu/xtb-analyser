name: cicd

on:
  push:
  workflow_dispatch:
    inputs:
      environment:
        description: "Select the deployment environment"
        required: true
        type: choice
        default: "development"
        options:
          - "development"

      force_deploy:
        description: "Force deployment even if no changes detected"
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
        # backend
      - run: bun install --frozen-lockfile
        working-directory: ./apps/backend/
      - run: bun test
        working-directory: ./apps/backend/
      - run: bun run build
        working-directory: ./apps/backend/
        # frontend
      - run: bun install --frozen-lockfile
        working-directory: ./apps/frontend/
      - run: bun test
        working-directory: ./apps/frontend/
      - run: bun run build:zola
        working-directory: ./apps/frontend/
        env:
          BACKEND_HOST: "https://api.dev.xtb-analyser.com/"
        # deploy
      - name: prepare build
        if: github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.force_deploy)
        run: |
          SHA_SHORT=$(git rev-parse --short HEAD)
          BUILD_TIME=$(date +"%Y-%m-%d %H:%M:%S %Z")
          echo "build_time = \"$BUILD_TIME\"" >> build_info.toml
          echo "version = \"$SHA_SHORT\"" >> build_info.toml
        working-directory: ./apps/frontend2/
      - name: build static website
        if: github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.force_deploy)
        run: docker run -v $PWD:/app --workdir /app ghcr.io/getzola/zola:v0.21.0 build
        working-directory: ./apps/frontend2/
      - name: Upload build-artifacts
        if: github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.force_deploy)
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build-artifacts
          path: |
            ./apps/frontend2/public/

  trigger-deploy-backend:
    needs: build
    if: github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.force_deploy)
    uses: ./.github/workflows/deploy_backend.yml
    secrets: inherit

  trigger-deploy-frontend:
    needs: trigger-deploy-backend
    if: github.ref == 'refs/heads/main' || (github.event_name == 'workflow_dispatch' && github.event.inputs.force_deploy)
    uses: ./.github/workflows/deploy_frontend.yml
    secrets: inherit
